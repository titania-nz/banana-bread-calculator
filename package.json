{
  "name": "banana-bread-calculator",
  "version": "1.0.0",
  "description": "A simple React + TypeScript application that calculates ingredient amounts for banana bread.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.8",
    "typescript": "^5.0.0"
  }
}
